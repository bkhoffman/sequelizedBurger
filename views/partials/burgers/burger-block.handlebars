<div class="mt-4">
	<h4>{{id}}. {{burger_name}}</h4>

	<!--Devour me button-->
	<button class="change-devour btn btn-primary" data-id="{{id}}" data-newdevour="{{devour}}"><i class="fas fa-hamburger"></i> Eat me
	</button>
</div>
